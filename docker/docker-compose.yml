services:
  hcm:
    container_name: hcm
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: host-credential-manager-go:latest
    network_mode: "host"
    volumes:
      - ../config:/root/config
      - ../cert:/root/cert
    restart: always
