version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: host-credential-manager-go:latest
    network_mode: "host"
    volumes:
      - ../data:/root/data
      - ../cert:/root/cert
    restart: always
